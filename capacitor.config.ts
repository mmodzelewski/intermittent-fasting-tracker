import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'dev.modzelewski.ift',
  appName: 'intermittent-fasting-tracker',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
